{
  "name": "corbett-bakers",
  "private": true,
  "version": "0.0.0",
  "description": "Corbett Bakers - Frontend & Backend Monorepo",
  "scripts": {
    "frontend:dev": "cd frontend && npm install && npm run dev",
    "frontend:build": "cd frontend && npm install && npm run build",
    "backend:dev": "cd backend && npm install && npm run dev",
    "backend:start": "cd backend && npm install && npm start",
    "install": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently \"npm run frontend:dev\" \"npm run backend:dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
